/**
 * The Decorator Pattern uses composition instead of inheritance to extend the
 * functionality of an object at runtime. At the same time, other instances of
 * the same class will not be affected by this, so individual object gets the
 * modified behavior.
 */
package net.ichatter.pattern.decorator;